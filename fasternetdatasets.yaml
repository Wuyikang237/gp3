# 数据集路径（绝对路径或相对路径）
path: /home/wuyikang/finalgp/ultralytics/datasets/jueyuanfirst # 数据集根目录
train: images/trains          # 训练图像目录（相对于 path）
val: images/vals              # 验证图像目录（相对于 path）

nc: 2
# 类别名称（顺序必须与标签中的 class_id 对应）

names: ['insulator', 'defect']
# ... 根据你的数据集添加